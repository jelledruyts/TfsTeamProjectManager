
namespace TeamProjectManager.Modules.WorkItemConfiguration
{
    // Note: the enum names must match the element names inside the XML definition.
    public enum ProcessProjectConfigurationPart
    {
        BugWorkItems,
        FeedbackRequestWorkItems,
        FeedbackResponseWorkItems,
        FeedbackWorkItems,
        PortfolioBacklogs,
        ReleaseStageWorkItems,
        ReleaseWorkItems,
        RequirementBacklog,
        StageSignoffTaskWorkItems,
        TaskBacklog,
        TypeFields,
        Weekends,
        WorkItemColors
    }
}