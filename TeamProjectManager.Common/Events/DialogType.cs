namespace TeamProjectManager.Common.Events
{
    /// <summary>
    /// Represents the possible dialog types that can be requested.
    /// </summary>
    public enum DialogType
    {
        /// <summary>
        /// The task history dialog.
        /// </summary>
        TaskHistory
    }
}